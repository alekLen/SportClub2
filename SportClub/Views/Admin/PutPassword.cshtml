@model SportClub.BLL.DTO.AdminDTO
@if (@Model == null)
{
    <span>Введен неверный пароль</span> <br />
}
else{
    <div style="background-color: #374566; box-shadow: inset 0 -3em 3em rgba(0, 0, 0, 0.1), 0.3em 0.3em 1em black; border-radius: 15px; width: 300px; margin-left: 400px; padding:20px">
<form  method="post">
    <input type="hidden"  value="@Model.Id" name="id" />   
     <p class="font" style="font-size: 32px; color: white; padding-top: 30px; text-align: center">Введите свой старый пароль</p>
            <input class="form-control font" type="password" name="pass" /><br />
            <input type="submit" formaction="PutPassword" value="ПОДТВЕРДИТЬ" class="y btn btn-primary font " style="color: white; font-weight:bold" />
</form>
</div>
}
