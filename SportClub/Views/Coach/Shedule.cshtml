@model MakeSheduleView
@{
    ViewData["Title"] = "Timetables";
}

@* <div class=" center1 padd">
    <div style="padding-right:80px">
        <a asp-area="" asp-controller="Users" asp-action="BackToRooms" class="btn btn-primary padd1 buttons1">@Resources.Resource.Back</a>
    </div>
    <input type="hidden" id="idshed" value="@Model.room.sheduleId" />
    <input type="hidden" id="idroom" value="@Model.room.Id" />
    <h1>@Model.room.Name &nbsp;&nbsp;&nbsp;</h1>
    <img src="@Model.room.Photo" style="width:70px;height:70px" alt="photo" />

</div>
@if (Model.message == null)
{
    <table style=" border: 1px solid black; margin-left:-100px;">
        <thead>
            <tr>
                <td><h6 class="padd1 white marg ">Понедельник</h6></td>
                <td><h6 class="padd1 white marg ">Вторник </h6></td>
                <td><h6 class="padd1 white marg ">Среда</h6></td>
                <td><h6 class="padd1 white marg ">Четверг</h6></td>
                <td><h6 class="padd1 white marg ">Пятница</h6></td>
                <td><h6 class="padd1 white marg ">Суббота</h6></td>
                <td><h6 class="padd1 white marg ">Воскресенье</h6></td>
            </tr>
        </thead>
        <tbody>
            <tr>
                @for (var t1 = 0; t1 < Model.times.Count; t1++)
                {

                    <td>

                        @for (var t = 0; t < Model.times[t1].Times.Count; t++)
                        {

                            <form method="post" class="padd1 marg">

                                <input type="hidden" name="day" value="@t1" />
                                <input type="hidden" name="roomId" value="@Model.room.Id" />
                                <input type="hidden" name="roomName" value="@Model.room.Name" />

                                <input type="text" name="time" value="@Model.times[t1].Times[t]" style="width:120px; background-color:yellow;font-size:20px;" readonly> <br />


                                @foreach (var it in Model.trainingInd)
                                {
                                    @if (@Model.times[t1].Times[t] != "Выходной" && @it.Time == @Model.times[t1].Times[t] && @it.Day == @t1 && it.RoomId == @Model.room.Id)
                                    {
                                        Model.flag = true;
                                        <div id=@t1+@Model.times[t1].Times[t]+@t1 class="flex">
                                            <div>
                                                <span class="white">Инд.тренировка</span><br />
                                                @if (it.CoachId.ToString() == ViewBag.MyId)
                                                {
                                                    <span class="white" style="color:coral;font-size:15px;font-weight:bold">Ваша тренировка</span>
                                                    <br />
                                                    @if (it.UserId == null)
                                                    {
                                                        <span class="white">Клиенты не записаны</span>
                                                    }
                                                    else
                                                    {
                                                        <span class="white">@it.UserName</span>
                                                    }
                                                }
                                                else
                                                {
                                                    <span class="white">Тренер : </span>

                                                    <br />
                                                    <span class="white">@it.CoachName</span>

                                                    <br />
                                                }
                                            </div>

                                        </div>

                                    }
                                }

                                > Іскра Олександр:
                                @foreach (var it in Model.traininggroup)
                                {
                                    @if (@Model.times[t1].Times[t] != "Выходной" && @it.Time == @Model.times[t1].Times[t] && @it.Day == @t1 && it.Room.Id == @Model.room.Id)
                                    {
                                        Model.flag = true;
                                        <div id=@t1+@Model.times[t1].Times[t]+@t1 class="flex">
                                            <div>
                                                @{
                                                    var num = it.Number - it.Users.Count;
                                                }
                                                <span class="white"> ( @it.Number )</span><br />

                                                @if (it.Coach.Id.ToString() == ViewBag.MyId)
                                                {
                                                    <span class="white" class="white" style="color:coral;font-size:15px;font-weight:bold">Ваша тренировка</span>
                                                    <br />
                                                    @if (it.Users.Count > 0)
                                                    {

                                                        @for (var index = 1; index <= it.Users.Count; index++)
                                                        {
                                                            <span class="white">@index . @it.Users[index - 1].Name</span>

                                                            <br />
                                                        }
                                                    }
                                                }
                                                else
                                                {
                                                    <span class="white">Тренер : </span>

                                                    <br />
                                                    <span class="white">@it.Coach.Name</span>

                                                    <br />
                                                }
                                            </div>

                                        </div>
                                    }
                                }

                            </form>

                        }

                    </td>

                }

            </tr>

        </tbody>
    </table>
}
@if (Model.message != null)
{
    <div class="center">
        <div style="display:flex;flex-direction:column;">
            <div>
                <h1>@Model.message</h1><div style="display:flex;flex-direction:column;">
                </div><br /><br />
                <div>
                    <form method="post">

                        <input type="hidden" name="Id" value="@Model.room.Id" />
                        <input type="submit" asp-controller="Time" asp-action="GetAllTimetable" class="padd1 white" value="Составить расписание зала" />

                    </form>
                </div>
            </div>
        </div>
    </div>
} *@



<div class="div_reg2 flex" style="justify-content:space-around;border:2px solid dimgray">


    <div style="padding-top:15px;">
        <a class="btn btn-outline-dark buttonsGY" style="padding:2px;border-radius:5px;min-width:120px;text-align:center;max-height:30px;" asp-area="" asp-controller="Time" asp-action="BackToRooms" class="btn btn-primary padd1 buttons1">@Resources.Resource.Back</a>
    </div>
    <div class="flex" style="flex-direction:column;">
        <div class="flex">
            <div style="min-width:40px;max-width:40px;max-height:7px;background-color:#a758bf;margin:5px;"></div>
            <div class="whiteY">@Resources.Resource.IndTr</div>
        </div>
        <div class="flex">
            <div style="min-width:40px;max-width:40px;max-height:7px;background-color:coral;margin:5px;"></div>
            <div class="whiteY">@Resources.Resource.GrTr</div>
        </div>
    </div>
    <input type="hidden" id="idshed" value="@Model.room.sheduleId" />
    <input type="hidden" id="idroom" value="@Model.room.Id" />
    <h1 class="whiteY">@Model.room.Name &nbsp;&nbsp;&nbsp;</h1>
    <img src="@Model.room.Photo" style="width:100px;height:auto" alt="photo" />
</div>
@if (Model.message == null)
{
    <div class="div_reg2_1">
        @for (var t1 = 0; t1 < Model.times.Count; t1++)
        {
            <div class="flex div_reg2_1" style=" border:2px solid dimgray">
                <div class="flex" style="flex-direction:column;justify-content:center">
                    @if (t1 == 0)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Пн</h2>
                        </div>
                    }
                    @if (t1 == 1)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Вт</h2>
                        </div>
                    }
                    @if (t1 == 2)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Ср</h2>
                        </div>
                    }
                    @if (t1 == 3)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Чт</h2>
                        </div>
                    }
                    @if (t1 == 4)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Пт</h2>
                        </div>
                    }
                    @if (t1 == 5)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Сб</h2>
                        </div>
                    }
                    @if (t1 == 6)
                    {
                        <div style="max-width:100px;min-width:100px">
                            <h2 class="padd whiteY marg div_reg2_1" style="text-align:center;">Вс</h2>
                        </div>
                    }

                </div>


                <div class="flex" style="flex-wrap:wrap!important; ">


                    @for (var t = 0; t < Model.times[t1].Times.Count; t++)
                    {
                        <div>
                            <form method="post">
                                <input type="hidden" name="day" value="@t1" />
                                <input type="hidden" name="roomId" value="@Model.room.Id" />
                                <input type="hidden" name="roomName" value="@Model.room.Name" />
                                <input type="hidden" name="time" value="@Model.times[t1].Times[t]" />

                                @foreach (var it in Model.trainingInd)
                                {
                                    <div class="info">
                                        @if (@Model.times[t1].Times[t] != "Выходной" && @it.Time == @Model.times[t1].Times[t] && @it.Day == @t1 && it.RoomId == @Model.room.Id)
                                        {
                                            Model.flag = true;

                                            <div id=@t1+@Model.times[t1].Times[t]+@t1 class="inner-info sh_card2">

                                                <div style="min-width:20px;max-width:20px;min-height:6px;background-color:#a758bf;"></div>
                                                <h3 class="sh_h3">@Model.times[t1].Times[t]</h3>
                                                @if (it.CoachId.ToString() == ViewBag.MyId)
                                                {
                                                    <div class="div_coach">
                                                        <span style="padding: 3px;margin-left:0px">Ваша тренировка</span>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <span>Тренер: <br></span>
                                                    <div class="div_coach" style="margin-left:0px;margin-top:3px">
                                                        <img src="@it.CoachPhoto" alt="photo">
                                                        <span style="padding: 3px;">@it.CoachName</span>
                                                    </div>
                                                }

                                                @* @if (it.UserId == null)
                                                {
                                                    <span style="display:block;text-align:center;font-weight:bold">
                                                        Нет клиента
                                                    </span>
                                                } *@
                                            </div>
                                            <div class="inner-info sh_card2" style="display:none;">
                                                <div style="min-width:20px;max-width:20px;min-height:6px;background-color:#a758bf;"></div>
                                                @if (it.UserId == null)
                                                {
                                                    <div class="div_a_btn " style="text-align:center;font-size:18px;margin-top:25px">
                                                        <span>Клиент не записан</span>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <p class="count_p" style="margin-bottom: -3px;height:48px;display:block">
                                                        <strong>Клиент:<br /></strong>@it.UserName
                                                    </p>

                                                }

                                            </div>

                                        }
                                    </div>
                                }
                                @foreach (var it in Model.traininggroup)
                                {
                                    <div class="info">
                                        @if (@Model.times[t1].Times[t] != "Выходной" && @it.Time == @Model.times[t1].Times[t] && @it.Day == @t1 && it.Room.Id == @Model.room.Id)
                                        {
                                            Model.flag = true;
                                            <div class="inner-info sh_card2">

                                                <div style="min-width:20px;max-width:20px;min-height:6px;background-color:coral;"></div>
                                                <h3>@Model.times[t1].Times[t]</h3>
                                                <p class="count_p" style="margin-left:0px; margin-bottom:3px"> <strong>Мест занято:</strong> @it.Users.Count/@it.Number </p>
                                                @if (it.Coach.Id.ToString() == ViewBag.MyId)
                                                {
                                                    <div class="div_coach" style="margin-top: 10px">
                                                        <span style="padding: 3px;margin-left:4px;">Ваша тренировка</span>
                                                    </div>
                                                }
                                                else
                                                {
                                                    <span style="margin-top:-3px">Тренер: <br></span>
                                                    <div class="div_coach" style="margin-left:0px;margin-top:3px">
                                                        <img src="@it.Coach.Photo" alt="photo">
                                                        <span style="padding: 2px;">@it.Coach.Name</span>
                                                    </div>
                                                }
                                            </div>
                                            <div class="inner-info sh_card2" style="display:none;">
                                                <div style="min-width:20px;max-width:20px;min-height:6px;background-color:coral;"></div>
                                                @if (it.Users.Count > 0)
                                                {
                                                    <div class="flex" style="height:125px;max-width:150px;max-height:125px;flex-direction:column;justify-content:center; overflow-y: scroll;overflow-x: hidden;">

                                                        <div class="whiteY" style="text-align:center;font-size:16px;">Клиенты:</div>

                                                        @foreach (var a in it.Users)
                                                        {
                                                            <div class="whiteY" style="text-align:center;max-width:140px;">@a.Name</div>
                                                        }
                                                    </div>
                                                }
                                                else
                                                {
                                                    <p>Клиентов нет</p>
                                                }
                                            </div>
                                        }
                                    </div>
                                }
                                @if (@Model.times[t1].Times[t] != "Выходной" && !@Model.flag)
                                {
                                    <div id=@t1+@Model.times[t1].Times[t] class="sh_card">
                                        <h3 style="margin-top: 20px;margin-left:7px;font-size: 26px;">@Model.times[t1].Times[t]</h3>
                                        <div class="div_a_btn1"> </div>
                                    </div>
                                }
                                @if (@Model.times[t1].Times[t] == "Выходной" && !@Model.flag)
                                {
                                    <div class="sh_card" style="height: 207.19px;">
                                        <h2 style="margin-top: 60px;margin-left: -7px;">@Resources.Resource.oDay</h2>
                                    </div>
                                }
                            </form>
                            @{
                                Model.flag = false;
                            }
                        </div>
                    }
                </div>
            </div>
        }
    </div>
}
@if (Model.message != null)
{
    <div class="center div_reg2" style="width:100%">
        <div style="display:flex;flex-direction:column;justify-content:space-around">
            <div>
                <h1 class="whiteY">@Resources.Resource.NoSh</h1>
            </div>
            <div class="flex padd" style="justify-content:center">
                <form method="post">

                    <input type="hidden" name="Id" value="@Model.room.Id" />
                    <input type="submit" asp-controller="Time" asp-action="GetAllTimetable"
                           class=" buttonsGY" value="@Resources.Resource.Add   @Resources.Resource.Shedule "
                           style="padding:6px;border-radius:4px;" />

                </form>
            </div>

        </div>
    </div>
}

<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script>
    $(document).ready(function () {

        var containerDivs = document.querySelectorAll('.info');

        containerDivs.forEach(function (container) {
            container.addEventListener('click', function () {

                var innerDivs = this.querySelectorAll('.inner-info');

                var visibleDiv = Array.from(innerDivs).find(div => div.style.display !== 'none');
                if (visibleDiv) {
                    visibleDiv.style.display = 'none';
                    var nextDiv = visibleDiv.nextElementSibling || innerDivs[0];
                    nextDiv.style.display = 'block';
                } else {
                    innerDivs[0].style.display = 'block';
                }
            });
        });


    });
</script>
<script src="~/lib/jquery/dist/jquery.min.js"></script>